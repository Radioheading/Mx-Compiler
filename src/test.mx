int main() {
    int a = getInt(), b = getInt(), c = getInt(), d = getInt();

    int sum = 0, j = 0, k = 0, l = 0;

    for (int i = 0; i < 10; ++i) {
        l = i + 1;
        j = 4 * i + 2;
        k = j + 1;
        sum = sum + k + l;
    }

    printlnInt(sum);
}